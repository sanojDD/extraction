[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "Playwright scraper for ekantipur.com — Audio Bee practical test"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.44.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
